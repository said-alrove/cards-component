@import "normalize.pcss";

:root {
    /* COLORS */
    --clr-pink-100: #DB008D;
    --clr-gray-100: #F2F2F2;
    /* VIEWPORT WIDTHS */
    --vw-sm: map(breakpoints, smartphone, md);
    --vw-md: map(breakpoints, tablet, sm);
    --vw-lg: map(breakpoints, tablet, lg);
}

@mixin test($bg-color, $color: blue) {
    background-color: $bg-color;
    color: $color;
}

size: $width, $height {
    inline-size: $width;
    block-size: $height;
}

$in-size: 50px;

html {
    font-size-responsive: 
        12px 40px 480px 1024px;
    size: 600px $in-size;
    $in-size: 100px;

    @at-root {
        h1 {
            font-size: $in-size;
            @include test(red);
            @util clearfix;
        }
    }
}

@media (min-width: var(--vw-sm)) {
    html { size: 300px 150px; }

    h1 { 
        //font-size: 35px; 
        margin: 5px * * 10px;
        -x-position: fixed 5px - - 10px;
        -x-color: #291ba1 #bba981;
    }

    div {
        margin-top: 50px;
        margin-left: 2;
    }
}